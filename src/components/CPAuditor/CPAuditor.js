import React from "react";
import "./CPAuditor.css";

function CPAuditor() {
  return (
    <section className="user">
      <nav>
        <h2>User Control Panel</h2>
        <button>Logout</button>
      </nav>
    </section>
  );
}

export default CPAuditor;
